# frozen_string_literal: true

RSpec.describe InvitationsController, type: :controller do
  it 'sends newsletter'
  it 'does not send newsletter twice'
  it 'does not allow duplicate newsletter for a community'
end
